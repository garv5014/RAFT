@page "/stock"
@using Raft_Library.Shop.shared

<h3>Stock of Product</h3>

<select @bind="selectedProduct">
    @foreach (var product in products)
    {
        <option value="@product.Name">@product.Name</option>
    }
</select>
<button @onclick="GetStock">View Stock</button>
<!-- Display stock and provide options to adjust -->
@code {
    string selectedProduct;
    int stock;
    private List<Product> products = new List<Product>
    {
        new Product { Name = "Water Bottle" },
        new Product { Name = "Notebook" },
        new Product { Name = "Pen" },
        new Product { Name = "T-shirt" },
        new Product { Name = "Coffee Mug" }
    };

    void GetStock()
    {
        // Placeholder for method to retrieve stock from backend
    }
}